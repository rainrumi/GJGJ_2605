# ステージクリアのリロール回数

## 目的と受入条件

- 各エリアのボス撃破回数が 1 / 2 / 3 回以上のとき、同じエリアのステージクリア画面で `RerollButton` をそれぞれ 1 / 2 / 5 回使える。
- ボス未撃破時と残回数が 0 のときは `RerollButton` を非表示にし、残回数がある場合に表示する。
- ボタン文言は `リロール(残り%d回)` とし、使用後に残回数を更新する。
- ステージクリア画面を開くたびに、その時点のエリア別ボス撃破回数から残回数を再設定する。

## 現状と設計

- `RunState.strengthened_enemy_defeat_counts` は `stage_id:stage_area` ごとにボス撃破を保持し、戦闘勝利時に更新される。
- `Main` はこの更新後に `StageClear.setup_clear_result()` を呼ぶ。
- `RunState` が日付変更直前にエリア別ボス撃破回数を 1 / 2 / 5 回のデータへ変換し、`Main` がそのデータを `StageClear` へ渡す。
- `StageClear` は渡されたリロール可能回数を画面ごとの状態へ反映して消費し、`StageClearUi` は既存の `RerollButton` の可視・無効状態と文言だけを表示する。
- 既存のデバッグ用無制限リロールを残し、通常の `RerollButton` は同じ候補再抽選処理を回数制限付きで呼び出す。

## 実装と検証

- [x] `RunState` にエリア別ボス撃破合算 API を追加する。
- [x] `Main` から勝利後の合算値をステージクリア画面へ渡す。
- [x] `StageClear` で 1 / 2 / 5 回へ変換、画面ごとの初期化、消費を実装する。
- [x] `StageClearUi` で `RerollButton` の表示・無効状態・残数テキストを更新する。
- [x] ボス未撃破、1 / 2 / 3 回、残数 0、画面再表示を検証する自動テストを追加する。
- [x] 差分、GDScript parse、関連テスト、ヘッドレス smoke を実行し、UIの目視確認可否を報告する。
