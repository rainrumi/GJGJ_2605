# 夢の種 説明・実装照合（2026-09-10）

状態: 一部修正済み、仕様確認待ち。

対象は `data/resources/seeds/seed_catalog.tres` に登録された26種類。ゲーム内表示の `main_description` / `sub_description` と、参照する効果Resource、Resolver、Game / Controllerの呼出経路を照合した。`resource/memo/DreamSeed/dream_seed_skills.md` は旧案と現在値が異なるため正本として使っていない。以下の「一致」はコード照合の結果であり、全組合せを実機操作したことを意味しない。

## 照合結果

|ID|名前|結果・残る確認事項|
|---|---|---|
|100101|キンセンカ|メイン+10%は一致。サブ+20%は次の消化1回のみ。持続期間を確認中。|
|100102|カモミール|クリア時+10%、自身の消化時+5%は定義・呼出経路で一致。|
|100103|トケイソウ|メイン-5%と重複制限は一致。サブ-15%は次の1回のみ。|
|100104|アネモネ|現在の説明・Resourceは蘇生回復+20%。既存テストは旧+50%で失敗。時間経過無効は次の蘇生1回のみで、期間を確認中。|
|100105|ホウセンカ|被ダメ+30%、受けたダメージ30%を次回加算。消化済み発生源が隣接検索から消える不具合を修正。サブの参照値は今回の消化バッチで受けたダメージであり、過去のターンを含む生涯累計ではない。|
|100106|ゲッカビジン|1時まで（境界含む）のクリアで永久+5%、重複制限あり。サブ50%短縮は次の1回のみ。|
|100107|ヨルガオ|クリア回復無効・4時以降追加選択は対応。サブ+200%は4時以降に消化した場合の次回のみ。|
|100108|フジ|メインの200%上昇を実装では2倍として扱う。サブは倍率1で変化せず、対象も悪夢を含む。仕様確認待ち。隣接検索の不発は共通修正済み。|
|100109|モウセンゴケ|全体消化+25%だった処理を悪夢への被消化倍率1.25へ修正。サブは消化後、胃内3体以下で+50%が戦闘中持続。回帰テストも現在の説明へ更新。|
|100110|ベラドンナ|表示計算で確率抽選し全対象へ適用していた処理を、悪夢の被消化計算時に抽選する処理へ修正。当選時3倍。サブ隣接倍率も悪夢に限定。|
|100111|マツヨイグサ|メインは現在の説明どおり3体以下で+50%。サブの分配対象から夢の種が除外される不具合を修正。|
|100112|ハス|メイン-20%・ライン+1、消化時ライン-1（最小1）は対応。|
|100113|アマリリス|胃袋X×Y×2の加算、消化時ライン内の悪夢に1000は対応。|
|100114|ノイバラ|メイン時間経過時500ダメージの関数が空。仕様確認待ち。サブ隣接1000の不発は修正。|
|100115|ユウガオ|横+1と重複制限は一致。サブ-10%は次の1回のみ。|
|100116|ルピナス|同種装備が1個のときだけ縦+2は現在の説明に一致。サブ+10%は次の1回のみ。|
|100117|ウイキョウ|時間進行ターンの開始時に胃内数×1%回復、自身の消化時に占有数×5%回復。時間経過後ではなく開始時に回復する点は挙動上の注意。|
|100118|ヤドリギ|メインは通常消化計算のダメージ総量×2%回復。サブが消化前の隣接オーラだった誤配線を、消化完了時に隣接対象へ倍率1.1を設定する既存効果に修正。|
|100119|ラフレシア|メインは悪夢消化時に悪夢最大HP20%回復（花の消化では発動しない）。サブは隣接消化ではなく自身消化で20%回復する。仕様確認待ち。|
|100120|ヒマワリ|悪夢消化時上限+5%は対応。自身消化時+10%が状態値だけ変わり実際の上限に反映されない不具合を修正。|
|100121|オトギリソウ|メインは即時攻撃ではなく次回消化への固定値加算。サブは現在HP×3ではなく固定300、対象も花を含む。仕様確認待ち。隣接検索の不発は共通修正済み。|
|100122|トリカブト|メインは直近の被ダメージ量を参照し、負の値を下限0で切り捨てるため実質無効。減少回数・累積仕様を確認中。サブ-50%の隣接不発は修正。|
|100123|ヒツジグサ|2時前の負の短縮率が0に切り捨てられる不具合を修正。2時前+50%、2時以降-50%。サブは消化時刻で決まる次の1回のみ。|
|100124|クレマチス|時間経過で短縮率を減算するため説明と逆方向。サブの持続登録もない。時間進行回数・持続仕様を確認中。|
|100125|トコン|メイン吐き戻し無傷は対応。サブの隣接悪夢吐き戻し不発を修正。ただし「被消化時に必ずHP0」は未実装で、通常ダメージでHP0になった場合のみ発動。確認中。|
|100126|ドクダミ|メイン吐き戻し時消化33%・プレイヤー最大HP50%ダメージに対応。サブの手動吐き戻し無効は戦闘中、消化+100%は次回のみ。期間確認中。|

## 設計判断

- 既存の効果ResourceとResolverの構成を維持した。新しいAutoload、Node、Sceneは追加していない。
- 隣接検索に消化済み発生源を許可する明示的な引数を追加し、種の消化完了効果だけが指定する。既存の悪夢の通常隣接検索は従来どおり。
- 説明と実装のどちらを正とするか判断が必要な変更は、ユーザーへ確認して保留した。
- 実装・検証にはリポジトリ指定の `skills/godot-implementation-check/SKILL.md` を使用した。

## 検証証拠

Godot: `C:\Program Files\Godot\Godot_v4.6.2-stable_win64.exe`、4.6.2 stable。PowerShellではGUI executableの終了を待つため `2>&1 | Out-String` で出力を受け、`$LASTEXITCODE` を確認した。パイプを付けず実行した初期の呼出しは検証成功の根拠に含めない。

以下は共通で `--headless --path .` を指定。

|確認|追加引数・コマンド|結果|
|---|---|---|
|差分|`git diff --check`|成功|
|Import|`--import`|終了0。UID警告と終了時の既存リソース解放エラーあり。|
|変更Script単体parse|変更された16本に `--script <path> --check-only`|15本は終了0。`game.gd` は単体検証時にAutoloadの `DebugState` を解決できず終了1。下記の全Script読込・戦闘起動で補完。|
|全Script読込|`--script tests/all_gdscript_parse_test.gd`|終了0、parse/compileエラーなし。終了時解放エラーあり。|
|新規回帰|`--script tests/dream_seed_description_contract_test.gd`|失敗0、終了0。ヒツジグサ境界、悪夢限定ダメージ、消化済み隣接、花への分配、Game上のヒマワリHP上限を検証。終了時解放エラーあり。|
|既存回帰|`--script tests/seed_100109_persistent_sub_effect_test.gd`|終了0|
|既存回帰|`--script tests/enemy_digestion_resolver_test.gd`|終了0|
|既存回帰|`--script tests/enemy_effect_stomach_activation_test.gd`|終了0|
|戦闘Scene起動|`--script tests/game_enemy_smoke_test.gd`|終了0。`res://scene/main/game/game.tscn` の起動・戦闘初期化。終了時解放エラーあり。|
|胃袋サイズ回帰|`--script tests/game_seed_stomach_base_size_test.gd`|終了0。終了時解放エラーあり。|
|Resource分類|`--script tests/seed_resource_classification_test.gd`|終了0|
|Main smoke|`--quit-after 120`|終了0。`res://scene/main/main.tscn`。終了時3 resources still in use。|
|既存不一致|`--script tests/anemone_revive_recovery_test.gd`|終了2。テストは旧50%を期待、現行説明・Resourceは20%。今回このテスト・数値は変更していない。|

ログは `.godot/agent-logs/seed-audit-*.log`。標準検証スクリプトはREADMEに記載されているが実ファイルが存在しないため、Godot CLIを直接実行した。formatter/linterの設定は確認できず未実行。C#なし。

## エラー切り分け・残リスク

- カタログの8件の古いUID参照は変更前のデータにも存在し、Godotは記載パスで読込を継続する。今回UIDは変更していない。
- `--verbose` で終了時の2リソースは未変更の `data/info/novel/novel_text_info.gd` と `data/info/stage/stage_info.gd` と特定。さらに `--script data/info/stage/stage_info.gd --check-only` 単独でも同じ解放エラーを再現した。種の回帰テスト失敗と区別する。
- Mainの `--quit-after 120 --verbose` も終了0で、追加の未解放リソースは未変更の `resource/sound/bgm/Night_Dance.mp3` と特定した。原因は未調査。終了0のみを理由にエラーなしとは扱わない。
- 表示・入力の手動確認は未実施。headlessでは視覚的正しさを保証しない。
- 確認待ちの効果は未修正のため、夢の種全体の説明と挙動の一致はまだ完了していない。
- 「総合消化ダメージ」の生涯累計か今回値か、ラフレシアの「モノ」が花も含むか、ウイキョウの時間進行内の回復順序、ヤドリギが追加攻撃・過剰ダメージまで回復に数えるかなど、文章だけで確定できない境界も残る。
