# 実装・検証報告

## 状態

- `<完了 / Partial / Blocked>`

## 変更

- `<path>`: <変更した挙動>

## 設計判断

- <既存方式へ合わせた点、採用理由>

## 検証

| 種別 | Command / 対象 | 結果 |
|---|---|---|
| Diff | `<command>` | `<pass/fail>` |
| Format/Lint | `<command>` | `<pass/fail/not configured>` |
| Import | `<command>` | `<pass/fail>` |
| Parse/Build | `<command>` | `<pass/fail/not applicable>` |
| Test | `<command / test name>` | `<pass/fail/not configured>` |
| Runtime | `<scene / flow>` | `<pass/fail/not run>` |
| Visual/Interaction | `<state / resolution / input>` | `<pass/fail/not run>` |

## 発生したFailureと対応

- <failure、原因、修正、再検証>

## 未検証・残リスク

- <未実行項目、理由、必要な次の操作>
